
#ifndef SCM_LDATA_HF_RAY_CAST_CONSTANTS_GLSLH_INCLUDED
#define SCM_LDATA_HF_RAY_CAST_CONSTANTS_GLSLH_INCLUDED

// global constants ///////////////////////////////////////////////////////////////////////////////
const int   max_binary_search_steps = 10;
const int   max_linear_search_steps = 10024;

const float epsilon         = 0.0001;

const vec3 white = vec3(1.0, 1.0, 1.0);
const vec3 black = vec3(0.0, 0.0, 0.0);
const vec3 red   = vec3(1.0, 0.0, 0.0);
const vec3 green = vec3(0.0, 1.0, 0.0);
const vec3 blue  = vec3(0.0, 0.0, 1.0);
const vec3 lblue = vec3(0.2, 0.7, 0.9);

#define SCM_SAFE_LINEAR_SEARCH 1
#undef SCM_SAFE_LINEAR_SEARCH

#endif // SCM_LDATA_HF_RAY_CAST_CONSTANTS_GLSLH_INCLUDED