# guacamole examples

set(LIB_PATH
  ${SCHISM_LIBRARY_DIRS}
  ${BOOST_LIBRARY_DIRS}
  ${BULLET_LIBRARY_DIRS}
  ${GUACAMOLE_LIBRARY_DIRS}
  ${ASSIMP_LIBRARY_DIRS}
  ${JSON_LIBRARY_DIRS}
  )
link_directories(${LIB_PATH})

set(LIBS
  ${SCHISM_LIBRARIES}
  ${BOOST_LIBRARIES}
  ${BULLET_LIBRARIES}
  ${GUACAMOLE_LIBRARIES}
  ${ASSIMP_LIBRARIES}
  ${JSON_LIBRARIES}
  )
#link_libraries(${LIBS})

set(INCLUDE_PATHS
  ${SCHISM_INCLUDE_DIRS}
  ${BOOST_INCLUDE_DIRS}
  ${BULLET_INCLUDE_DIRS}
  ${GUACAMOLE_INCLUDE_DIRS}
  ${ASSIMP_INCLUDE_DIRS}
  ${JSON_INCLUDE_DIRS}
  )

add_subdirectory(multicontext)
