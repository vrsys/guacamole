if (UNIX)
  if(GUACAMOLE_ENABLE_NVIDIA_3D_VISION)
  add_subdirectory(nvstusb)
  endif(GUACAMOLE_ENABLE_NVIDIA_3D_VISION)
  if(PLUGIN_guacamole-nrp)
  add_subdirectory(simpleini)
  endif(PLUGIN_guacamole-nrp)
endif(UNIX)
